<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <!-- System-audio capture uses AudioRecord, which counts as a "microphone" FGS on API 34+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LanMediaSender">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!-- No configChanges override: the activity recreates on window
                 resize/rotation so it can switch between the single-column and the
                 wide two-column (layout-w600dp) layout. Streaming runs in the
                 service, so recreation never interrupts a stream, and field values
                 persist via SharedPreferences. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Screen capture + audio capture run here so they survive the app going to the
             background. Both foreground-service types are declared; the service picks
             the right set at runtime (mediaProjection always; microphone only when
             audio is enabled and RECORD_AUDIO is granted). -->
        <service
            android:name=".SenderService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|microphone" />
    </application>
</manifest>
